import { AsSecuredPathPipe } from './as-secured-path.pipe';

describe('AsSecuredPathPipe', () => {
  it('create an instance', () => {
    const pipe = new AsSecuredPathPipe();
    expect(pipe).toBeTruthy();
  });
});
