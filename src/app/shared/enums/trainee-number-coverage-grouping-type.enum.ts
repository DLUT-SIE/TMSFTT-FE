/** Indicate the subOption of a trainee number or coverage statistics graph. */
export enum TraineeNumberCoverageGroupingType {
    BY_DEPARTMENT = 0,
    BY_STAFF_TITLE = 1,
    BY_AGE_DISTRIBUTION = 2
}
