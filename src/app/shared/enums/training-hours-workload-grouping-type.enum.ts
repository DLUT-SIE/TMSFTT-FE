/** Indicate the subOption of a training hours and workload statistics graph. */
export enum TrainingHoursWorkloadGroupingType {
    BY_TOTAL_STAFF_NUM = 0,
    BY_TOTAL_TRAINING_HOURS = 1,
    BY_PER_CAPITA_TRAINING_HOURS = 2,
    BY_TOTAL_WORKLOAD = 3,
    BY_PER_CAPITA_WORKLOAD = 4
}
