/** Indicate the subOption of a staff statistics graph. */
export enum StaffGroupingType {
    BY_DEPARTMENT = 0,
    BY_STAFF_TYPE = 1,
    BY_STAFF_TITLE = 2,
    BY_HIGHEST_DEGREE = 3,
    BY_AGE_DISTRIBUTION = 4
}
